Add express backend use ai-sdk to provide api `/api/chat`

- add `backend` folder to store backend code
- support human-in-the-loop interaction
- support mcp tool calling
- manage mcp json in settings page
- pass mcp json using custom body in `useChat`
- add backend start script into `package.json`
