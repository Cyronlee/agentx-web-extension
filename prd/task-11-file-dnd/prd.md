implement file drag and drop functionality for this web extension

- when user drag a file or image from computer or left side web page, the whole extension should display a `blue see-through mask` to indicate the drop area
- when user drop the file, the file should be inserted into the prompt input textarea
- also add a right click menu in the left side web page when user right click on a image, the menu should have an options `Send to AgentX` to insert the image into the prompt input textarea
